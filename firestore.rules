rules_version = '2';

// A user can only read and write their own document. Bladder-diary data is private.
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
